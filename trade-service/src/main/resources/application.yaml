server:
  port: 8084

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${hm.nacos.host}:${hm.nacos.port}

feign:
  okhttp:
    enabled: true

hm:
  swagger:
    title: "黑马商城交易管理接口文档"
    package: com.hmall.trade.controller
