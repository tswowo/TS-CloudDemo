server:
  port: 8085

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${ts.nacos.host}:${ts.nacos.port}

feign:
  okhttp:
    enabled: true

ts:
  swagger:
    title: "云商城支付管理接口文档"
    package: com.tscloud.pay.controller
