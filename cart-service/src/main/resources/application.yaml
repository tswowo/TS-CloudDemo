server:
  port: 8082
  tomcat:
    threads:
      max: 50 # 允许的最大线程数
    accept-count: 50 # 最大排队等待数量
    max-connections: 100 # 允许的最大连接

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${hm.nacos.host}:${hm.nacos.port}

feign:
  okhttp:
    enabled: true
  sentinel:
    enabled: true # 开启feign对sentinel的支持

hm:
  db:
    host: ${hm.db.host}
    port: ${hm.db.port}
    pw: ${hm.db.pw}
    database: ${hm.db.database}
  swagger:
    title: "黑马商城购物车服务接口文档"
    package: com.hm.cart.controller
